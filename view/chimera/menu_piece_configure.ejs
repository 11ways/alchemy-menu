<%
expands('chimera/menu_manager');

start('tempeditor')

var options = {};
options.groups = __groups;

if (typeof showSaveButton !== 'undefined') {
	options.showSaveButton = showSaveButton;
}

options.formName = 'MenuPiece';


print_element('chimera/edit_form', {variables: {options: options}});

end('tempeditor') %>